ISTIO_INJECT="${ISTIO_INJECT:-true}"
NAMESPACE="${NAMESPACE:-asm-perf}"
CONNECTIONS="${CONNECTIONS:-10}"
DURATION="${DURATION:-30s}"
DEPLOY_REPLICAS="${DEPLOY_REPLICAS:-2}"
QPS="${QPS:-1000}"
TIMEOUT="${TIMEOUT:-1200}"